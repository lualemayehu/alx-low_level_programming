#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Program prints alpahabates in lower case
 */

int main(void)
{
	char n = 'a';

	while (n < '{')
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
